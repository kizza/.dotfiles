#!/usr/bin/env bash

case "$(uname -s)" in
  Darwin)
    # ls /System/Library/Sounds (for sound names)
    osascript -e "display notification \"$2\" with title \"$1\" sound name \"Purr\""
    ;;
  Linux)
    notify-send "$1" "$2"
    ;;
esac
