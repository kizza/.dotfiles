# 22source ~/.rvm/scripts/rvm

#2 Add RVM to PATH for scripting. Make sure this is the last PATH variable change.
#2 export PATH="$PATH:$HOME/.rvm/bin"

#2 [[ -s "$HOME/.rvm/scripts/rvm" ]] && source "$HOME/.rvm/scripts/rvm" # Load RVM into a shell session *as a function*
