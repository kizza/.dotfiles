.DS_Store
.direnv/
.kizza
*.crt
*.key

# Per-worktree brief and running log, written by the `worktrees` skill. Never committed.
prd.md

# Local review remarks, read by the editor and by coding agents. Never committed.
.remarks/
